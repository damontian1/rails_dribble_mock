- if user_signed_in?
	%p#intro
		Hello
		= current_user.name
		%br
		%span This is a website to share your ideas
- else 
	%p#intro
		Share your ideas with the world
		%br
		%span This is a website to share your ideas

#posts
	- @posts.each do |post|
		.post
			.post_image
				= link_to (image_tag post.image.url(:small)), post
			.post_content
				.title
					%h2= link_to post.title, post
				.data.clearfix
					%p.username
						Shared By
						= post.user.name
					%p.buttons
						%span 
							%i.fa.fa-comments-o
							= post.comments.count
						%span
							%i.fa.fa-thumbs-o-up
							= post.get_upvotes.size