- @posts.each do |post|
	%h2= link_to post.title, post
	= image_tag post.image.url(:small), class: "image1" 
	%p= post.user.name

= link_to "Add New Inspiration", new_post_path
= link_to "Sign Out", destroy_user_session_path, method: :delete